import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CST Unified Message System",
  description: "Phase 1 — internal marketplace message review workspace",
};

export default function RootLayout({
  children,
}: {
  // Typed explicitly rather than via Next's build-generated `LayoutProps`, so
  // `npm run typecheck` works on a fresh clone before the first build.
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
